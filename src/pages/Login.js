import React, { Fragment } from 'react';

import { LoginForm } from '../components/LoginForm';

export const Login = () => {
  return (
    <Fragment>
      <LoginForm/>
      <h1>Login Page</h1>
    </Fragment>
  )
}
